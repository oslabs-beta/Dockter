import React from 'react';

const Filter = (props) => {
  return <div>Filter</div>;
};

//filter by:
//stream: stdout || stderr drop down menu?
//containerId: string input bar?
//timestamp -> how to convert into human readable(backend) and how to filter by
//filter by specific date/range of dates?
//what is log level -> info, error, fatal

export default Filter;
