import React, { useState } from 'react';

const LogsView = (props) => {
  const [logs, setLogs] = useState([]);

  //invokes handleView
  //receive array of objects
  //Log ID
  //containerID (foreingn key)
  //message
  //timestamp
  //loglevel

  return <div>LogsView</div>;
};

export default LogsView;
